<%= form_for @heroine do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <br>
  <%= f.label :super_name %>
  <%= f.text_field :super_name %>
  <br>
  <div>
  Power:
  <%= f.collection_select :power_id, Power.all, :id, :name %>
  <%= f.submit %>
  </div>
  
<% end %>